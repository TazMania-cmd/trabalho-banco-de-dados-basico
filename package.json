{
  "name": "sistema-locadora-supabase",
  "version": "1.0.0",
  "description": "Backend Node.js com Supabase e front-end usando Fetch API para consultar e manipular dados da locadora.",
  "main": "server.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "build": "node --check app.js && node --check server.js && node --check api/index.js && node --check src/auth/authService.js && node --check src/auth/supabaseSessionStore.js && node --check src/routes/authRoutes.js && node --check src/middlewares/authMiddleware.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.106.2",
    "bcryptjs": "^3.0.3",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "express-rate-limit": "^8.5.2",
    "express-session": "^1.19.0"
  }
}
